# Dev / test dependencies (install into the whisper venv or a fresh one)
-r requirements.txt
pytest>=8.0
httpx>=0.27
anyio>=4.0
pytest-asyncio>=0.24

# Shared library unit-test surface (no GPU required)
# Windows runtime deps listed in windows/requirements.txt
