# Dev / test dependencies (install into the whisper venv or a fresh one)
-r requirements.txt
pytest>=8.0
httpx>=0.27
anyio>=4.0
pytest-asyncio>=0.24
# Imported directly by the test suite / benchmark scripts it loads:
#   test_icon_assets.py -> PIL;  test_benchmark_helpers.py -> benchmark script -> requests
Pillow>=10.0
requests>=2.31

# Shared library unit-test surface (no GPU required)
# Windows runtime deps listed in windows/requirements.txt
